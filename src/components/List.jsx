import styled from 'styled-components';

const List = () => {
  return (
    <ListContainer>
      <div>List</div>
    </ListContainer>
  );
};

const ListContainer = styled.div`
  background-color: pink;
`;

export default List;
